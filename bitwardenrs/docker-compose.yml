version: "2"

services:
  bitwardenrs:
    image: bitwardenrs/server:latest
    container_name: bitwardenrs
    volumes:
      - $HOME/docker-data/bitwardenrs:/data/
    ports:
      - 8100:80
    restart: unless-stopped